# Fixture — all PASS

This file's only purpose is to be verified by the self-referential demo. It
makes one claim about verify_claims.py that always passes when verify_claims.py is intact.

The `parse_claims` function exists. <!-- claim: signature target=verify_claims.parse_claims has-params=text -->
