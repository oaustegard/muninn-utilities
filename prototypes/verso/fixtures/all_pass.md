# Fixture — all PASS

This file's only purpose is to be verified by the self-referential demo. It
makes one claim about verso.py that always passes when verso.py is intact.

The `parse_claims` function exists. <!-- claim: signature target=verso.parse_claims has-params=text -->
